<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="in.hocg.zhifou.mapper.PostMapper">

    <sql id="columns">
        ${alias}.*
    </sql>

    <select id="search" resultType="in.hocg.zhifou.pojo.vo.SearchPostVo">
        SELECT
        <include refid="columns">
            <property name="alias" value="tp"/>
        </include>
        FROM t_post tp
        LIMIT 10
    </select>

    <select id="findAllByCreatedDay" resultType="in.hocg.zhifou.domain.Post">
        SELECT
        <include refid="columns">
            <property name="alias" value="tp"/>
        </include>
        FROM t_post tp
        <where>
            <![CDATA[
                TO_DAYS(created_at) = TO_DAYS(#{day});
            ]]>
        </where>
    </select>

    <!--<select id="findAllByCreatedDay" resultType="in.hocg.zhifou.domain.Post">-->
        <!--SELECT-->
        <!--<include refid="columns">-->
            <!--<property name="alias" value="tp"/>-->
        <!--</include>-->
        <!--FROM t_post tp-->
        <!--<where>-->
            <!--<![CDATA[-->
                <!--TO_DAYS(created_at) <= TO_DAYS(#{day});-->
            <!--]]>-->
        <!--</where>-->
    <!--</select>-->
</mapper>